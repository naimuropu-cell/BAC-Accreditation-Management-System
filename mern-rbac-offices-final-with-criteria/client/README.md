# RBAC Offices Client (React + Vite + Tailwind + daisyUI)

## Quick Start
```bash
cp .env.example .env
npm install
npm run dev
```

Ensure the server is running on the URL configured in `VITE_API_BASE`.
